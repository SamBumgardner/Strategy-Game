package observerPattern.eventSystem;

/**
 * typeDef for the ActionEvent class, which is really just a wrapper around an int. 
 * When combined with the power of the EventExtender class, however, it gains the ability
 * 	to get and set smaller data fields within its 32-bit int size.
 * 
 * @author Samuel Bumgardner
 */
typedef InputEvent = Int;