package units;

/**
 * Used to identify a type of stat change.
 * 
 * Unlike most enums in this game, the order of these doesn't matter,
 * 	and you shouldn't have to change up other code if you add new
 * 	entries to this. 
 * 
 * Be careful when removing an enum, though, because it may be in use
 * 	somewhere.
 * 
 * @author Samuel Bumgardner
 */
enum StatChangeTypes 
{
	
}