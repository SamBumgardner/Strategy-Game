package units;

/**
 * Enum that defines the categories of units that matter to the MapCursor.
 * 
 * @author Samuel Bumgardner
 */
enum MapCursorUnitTypes
{
	PLAYER_ACTIVE;
	PLAYER_INACTIVE;
	NOT_PLAYER;
	NONE;
}