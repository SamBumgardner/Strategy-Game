package units;

/**
 * typeDef for the MoveID type, which is really just a wrapper around an int. 
 * When combined with the power of the MoveIDExtender class, however, it gains the ability
 * 	to store row and column information in a single integer in an easy-to-read manner.
 * 
 * @author Samuel Bumgardner
 */
typedef MoveID = Int;